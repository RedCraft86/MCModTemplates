modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="${mod_authors}"
credits="${mod_credits}"
description='''${mod_description}'''
displayURL="https://github.com/RedCraft86/${repo_name}"
issueTrackerURL="https://github.com/RedCraft86/${repo_name}/issues"
logoFile="${mod_id}_icon.png"
#updateJSONURL=""

[[accessTransformers]]
file="META-INF/accesstransformer.cfg"

[[mixins]]
config="${mod_id}.mixins.json"

[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="${neoforge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

# Some dependency
#[[dependencies.${mod_id}]]
#   modId="dependency_mod_id"
#   type="required"
#   versionRange="dependency_mod_version"
#   ordering="AFTER" # Load the mod {NONE, BEFORE, AFTER} this dependency
#   side="BOTH"

[modproperties.${mod_id}] # NOTE, Catalogue uses logoFile above for banner, max height is 240
catalogueImageIcon="${mod_id}_icon.png"
